
name: CI

on:
  push:
    branches:
    - main
  schedule:
  - cron: "0 16 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run
        run: |
          apt isntall python3
          python3 run.py
